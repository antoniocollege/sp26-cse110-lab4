1. Console prints 3
2. Console prints 150
3. Console prints 150
4. It will return the array [50, 100, 150]
5. console.log(i) crashes because i is not defined
6. console.log(discountedPrice) crashes because discountedPrice is not defined
7. Console prints 150
8. It will return the array [50, 100, 150]
9. console.log(i) crashes because i is not defined
10. Console prints 3
11. It will return the array [50, 100, 150]

12a. student.name

12b. student['Grad Year']

12c. student.greeting();

12d. student['Favorite Teacher'].name

12e. student.courseLoad[0]

13a. 32 (interprets 2 as a string)

13b. 1 (numeric subtraction, converts '3' to a number)

13c. 3 (null is converted to 0 with numbers)

13d. 3null (+ with a string means concatenate)

13e. 4 (true is converted to 1 with numbers)

13f. 0 (false and null map to 0)

13g. 3undefined (string with a plus means concatenate)

13h. NaN (- means numeric subtraction, undefined turns into NaN)

14a. true (converted to numerical)

14b. false (both are strings so it uses lexicographic comparison)

14c. true (loose equality, lets use type coercion)

14d. false (strict equality, no type coercion)

14e. false (loose equality, true is converted to 1)

14f. true (boolean of non-zero numbers equals true, types are true)

15. The == operator compares for equality after doing type conversions. The === operator will not do the conversion and will simply compare the two as they currently exist.

16. 
17. 
18. 
19. 
